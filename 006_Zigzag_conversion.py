'''
Created on Dec 8, 2016

@author: anand
'''

def convert(s, numRows):
    """
    :type s: str
    :type numRows: int
    :rtype: str
    """
    
    currRow = 0
    currCol = 0  # assume 1st col is even, not odd
    opMatrix = []
    
    for index in range(numRows):
        opMatrix.append([])
        
    
    while c in s:
        
        if currCol %2 ==0:  # ODD, with my logic
            maxRow = numRows-1
        else:
            maxRow =     
            
    